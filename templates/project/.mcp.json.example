{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "${PLAYWRIGHT_MCP_PATH}",
      "args": [
        "--headless"
      ],
      "env": {}
    },
    "postgres": {
      "type": "stdio",
      "command": "${POSTGRES_MCP_PATH}",
      "args": [],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    }
  }
}
